
@extends('layouts.app')

@section('title', 'Gestión de Cargos - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/cargo.css') }}">
@endpush

@section('content')
<!-- Cabecera de Contenido -->
<header class="main-header">
    <div class="header-welcome">
        <h1>Cargos de la Institución</h1>
        <p>Gestión y configuración de los roles de los usuarios (Coordinador, Asistente, Docente, etc.)</p>
    </div>
    <div class="header-date">
        <i class="fa-regular fa-calendar"></i>
        <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
    </div>
</header>

<!-- Contenedor de Notificaciones/Alertas de Laravel (Flotante Tipo Toast) -->
<div class="alerts-container">
    @if(session('success'))
        <div class="alert-message success-alert">
            <i class="fa-solid fa-circle-check"></i>
            <span>{{ session('success') }}</span>
        </div>
    @endif

    @if(session('error'))
        <div class="alert-message error-alert">
            <i class="fa-solid fa-circle-xmark"></i>
            <span>{{ session('error') }}</span>
        </div>
    @endif

    @if($errors->any())
        <div class="alert-message error-alert">
            <i class="fa-solid fa-triangle-exclamation"></i>
            <ul style="margin: 0; padding-left: 1.2rem;">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
</div>

<!-- Contenedor Principal de la Tabla -->
<section class="crud-container">
    
    <!-- Barra Superior de Acciones Filtros (Buscar y Agregar) -->
    <div class="table-actions-bar">
        <div class="search-wrapper">
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
            <input type="text" placeholder="Buscar cargo..." class="search-input" id="search-cargo" autocomplete="off">
        </div>

        <div class="filters-wrapper">
            <select class="action-select" id="filter-status">
                <option value="all">Todos los estados</option>
                <option value="active">Habilitados</option>
                <option value="inactive">Inhabilitados</option>
            </select>

            <button class="btn-primary-action" id="btn-open-create">
                <i class="fa-solid fa-plus"></i> Agregar Cargo
            </button>
        </div>
    </div>

    <!-- Tabla de Datos -->
    <div class="table-responsive">
        <table class="crud-table">
            <thead>
                <tr>
                    <th width="100px">ID</th>
                    <th>Nombre del Cargo</th>
                    <th>Estado</th>
                    <th width="180px">Acción</th>
                </tr>
            </thead>
            <tbody id="table-body">
                @forelse($cargos as $cargo)
                    <tr class="data-row" 
                        data-nombre="{{ strtolower($cargo->nombre) }}"
                        data-estado="{{ $cargo->estado ? 'active' : 'inactive' }}">
                        <td><strong>{{ $cargo->id }}</strong></td>
                        <td>
                            <div class="table-primary-text">{{ $cargo->nombre }}</div>
                            <span class="table-secondary-text">Cod: CARG-{{ str_pad($cargo->id, 3, '0', STR_PAD_LEFT) }}</span>
                        </td>
                        <td>
                            @if($cargo->estado)
                                <span class="status-badge status-active">Habilitado</span>
                            @else
                                <span class="status-badge status-inactive">Inhabilitado</span>
                            @endif
                        </td>
                        <td>
                            <div class="action-buttons">
                                <button class="btn-action-edit" onclick="openEditModal('{{ $cargo->id }}', '{{ $cargo->nombre }}', '{{ $cargo->estado ? 'active' : 'inactive' }}')">
                                    <i class="fa-solid fa-pen"></i> Editar
                                </button>
                                <button class="btn-action-delete" onclick="openDeleteModal('{{ $cargo->id }}', '{{ $cargo->nombre }}')">
                                    <i class="fa-solid fa-ban"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                @empty
                    <tr class="empty-state-row" id="empty-row">
                        <td colspan="4" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No se encontraron cargos registrados.
                        </td>
                    </tr>
                @endforelse
                <!-- Fila defensiva para búsquedas vacías en JS -->
                <tr class="empty-state-row" id="no-results-row" style="display: none;">
                    <td colspan="4" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                        <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                        No se encontraron resultados que coincidan con la búsqueda.
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    <!-- Soporte defensivo de paginación unificado -->
    @if(method_exists($cargos, 'hasPages') && $cargos->hasPages())
        <div class="pagination-container" style="margin-top: 15px;">
            {{ $cargos->links() }}
        </div>
    @endif
</section>

<!-- MODAL PARA CREAR -->
<div class="modal-overlay" id="create-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Registrar Cargo</h3>
        </div>
        <form id="create-form" action="{{ route('cargo.store') }}" method="POST">
            @csrf
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-briefcase input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-nombre-cargo">Nombre del Cargo</label>
                        <input type="text" id="create-nombre-cargo" name="nombre" placeholder="Ej. Coordinador" required autocomplete="off">
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-estado">Estado</label>
                        <select id="create-estado" name="estado" required>
                            <option value="active" selected>Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Registrar</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL PARA MODIFICAR -->
<div class="modal-overlay" id="edit-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Modificar Cargo</h3>
        </div>
        <form id="edit-form" method="POST">
            @csrf
            @method('PUT')
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-briefcase input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-nombre-cargo">Nombre del Cargo</label>
                        <input type="text" id="edit-nombre-cargo" name="nombre" required autocomplete="off">
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-estado">Estado</label>
                        <select id="edit-estado" name="estado" required>
                            <option value="active">Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL DE INHABILITACIÓN -->
<div class="modal-overlay" id="delete-modal">
    <div class="modal-container modal-danger-layout">
        <form id="delete-form" method="POST">
            @csrf
            @method('DELETE')
            <div class="modal-header">
                <h3>Inhabilitar Cargo</h3>
            </div>
            <div class="modal-body text-center">
                <div class="danger-icon-wrapper">
                    <i class="fa-solid fa-circle-exclamation"></i>
                </div>
                <p class="delete-warning-text">
                    ¿Está seguro de que desea inhabilitar el cargo <strong id="delete-item-name"></strong>?
                </p>
                <p class="delete-sub-text">Esta acción cambiará el estado de este cargo a inhabilitado.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
            </div>
        </form>
    </div>
</div>
@endsection

@push('scripts')
    <!-- Script propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/cargo.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Gestión de Carreras - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/carrera.css') }}">
@endpush

@section('content')
<!-- Cabecera de Contenido -->
<header class="main-header">
    <div class="header-welcome">
        <h1>Gestión de Carreras</h1>
        <p>Configuración y administración de programas de estudio y carreras ofertadas en la institución</p>
    </div>
    <div class="header-date">
        <i class="fa-regular fa-calendar"></i>
        <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
    </div>
</header>

<!-- Contenedor de Alertas de Laravel -->
<div class="alerts-container">
    @if(session('success'))
        <div class="alert-message success-alert">
            <i class="fa-solid fa-circle-check"></i>
            <span>{{ session('success') }}</span>
        </div>
    @endif

    @if(session('error'))
        <div class="alert-message error-alert">
            <i class="fa-solid fa-circle-xmark"></i>
            <span>{{ session('error') }}</span>
        </div>
    @endif

    @if($errors->any())
        <div class="alert-message error-alert">
            <i class="fa-solid fa-triangle-exclamation"></i>
            <ul style="margin: 0; padding-left: 1.2rem;">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
</div>

<!-- Contenedor Principal de la Tabla -->
<section class="crud-container">
    
    <!-- Barra Superior de Acciones Filtros (Buscar, Filtrar y Agregar) -->
    <div class="table-actions-bar">
        <div class="search-wrapper">
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
            <input type="text" placeholder="Buscar carrera, código o descripción..." class="search-input" id="search-carrera" autocomplete="off">
        </div>

        <div class="filters-wrapper">
            <!-- Valores con 1 y 0 para coincidir con la lógica del JS -->
            <select class="action-select" id="filter-status">
                <option value="all">Todos los estados</option>
                <option value="1">Habilitadas</option>
                <option value="0">Inhabilitadas</option>
            </select>

            <button class="btn-primary-action" id="btn-open-create">
                <i class="fa-solid fa-plus"></i> Agregar Carrera
            </button>
        </div>
    </div>

    <!-- Tabla de Datos -->
    <div class="table-responsive">
        <table class="crud-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Código</th>
                    <th>Carrera / PNF</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th width="150px">Acción</th>
                </tr>
            </thead>
            <tbody id="table-body">
                @forelse($carreras as $carrera)
                <tr class="data-row" 
                    data-nombre="{{ strtolower($carrera->nombre) }}" 
                    data-codigo="{{ strtolower($carrera->codigo) }}" 
                    data-descripcion="{{ strtolower($carrera->descripcion ?? '') }}" 
                    data-estado="{{ $carrera->estado ? '1' : '0' }}">
                    <td><strong>{{ $carrera->id }}</strong></td>
                    <td>
                        <span class="status-badge" style="background-color: var(--color-hover-bg); color: var(--color-brand-blue); border: 0.08rem solid var(--color-border);">
                            {{ $carrera->codigo }}
                        </span>
                    </td>
                    <td>
                        <div class="table-primary-text">{{ $carrera->nombre }}</div>
                    </td>
                    <td>{{ $carrera->descripcion ?? 'Sin descripción disponible.' }}</td>
                    <td>
                        @if($carrera->estado)
                            <span class="status-badge status-active">Habilitado</span>
                        @else
                            <span class="status-badge status-inactive">Inhabilitado</span>
                        @endif
                    </td>
                    <td>
                        <div class="action-buttons">
                            <button class="btn-action-edit" 
                                onclick="openEditModal('{{ $carrera->id }}', '{{ addslashes($carrera->nombre) }}', '{{ addslashes($carrera->codigo) }}', '{{ addslashes($carrera->descripcion ?? '') }}', '{{ $carrera->estado ? '1' : '0' }}')">
                                <i class="fa-solid fa-pen"></i> Editar
                            </button>
                            <button class="btn-action-delete" 
                                onclick="openDeleteModal('{{ $carrera->id }}', '{{ addslashes($carrera->nombre) }}')" 
                                title="Inhabilitar Carrera">
                                <i class="fa-solid fa-ban"></i>
                            </button>
                        </div>
                    </td>
                </tr>
                @empty
                <tr class="empty-state-row" id="empty-row">
                    <td colspan="6" style="text-align: center; color: var(--color-text-muted); padding: 3rem;">
                        <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                        No hay carreras registradas aún.
                    </td>
                </tr>
                @endforelse
                <!-- Fila defensiva para búsquedas vacías en JS -->
                <tr class="empty-state-row" id="no-results-row" style="display: none;">
                    <td colspan="6" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                        <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                        No se encontraron resultados que coincidan con la búsqueda.
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    @if(method_exists($carreras, 'hasPages') && $carreras->hasPages())
        <div class="pagination-container" style="margin-top: 15px;">
            {{ $carreras->links() }}
        </div>
    @endif
</section>

<!-- MODAL PARA CREAR -->
<div class="modal-overlay" id="create-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Registrar Carrera</h3>
        </div>
        <form id="create-form" action="{{ route('carrera.store') }}" method="POST">
            @csrf
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-graduation-cap input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-nombre">Nombre de la Carrera / PNF</label>
                        <input type="text" id="create-nombre" name="nombre" placeholder="Ej. Ingeniería en Informática" required autocomplete="off">
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-font input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-codigo">Código o Acrónimo</label>
                        <input type="text" id="create-codigo" name="codigo" placeholder="Ej. ING-INF" required autocomplete="off" style="text-transform: uppercase;">
                    </div>
                </div>

                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="create-descripcion">Descripción</label>
                        <textarea id="create-descripcion" name="descripcion" placeholder="Descripción breve del plan de estudios..." rows="3"></textarea>
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-estado">Estado</label>
                        <select id="create-estado" name="estado" required>
                            <option value="1" selected>Habilitado</option>
                            <option value="0">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Registrar</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL PARA MODIFICAR -->
<div class="modal-overlay" id="edit-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Modificar Carrera</h3>
        </div>
        <form id="edit-form" method="POST">
            @csrf
            @method('PUT')
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-graduation-cap input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-nombre">Nombre de la Carrera / PNF</label>
                        <input type="text" id="edit-nombre" name="nombre" required autocomplete="off">
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-font input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-codigo">Código o Acrónimo</label>
                        <input type="text" id="edit-codigo" name="codigo" required autocomplete="off" style="text-transform: uppercase;">
                    </div>
                </div>

                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="edit-descripcion">Descripción</label>
                        <textarea id="edit-descripcion" name="descripcion" rows="3"></textarea>
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-estado">Estado</label>
                        <select id="edit-estado" name="estado" required>
                            <option value="1">Habilitado</option>
                            <option value="0">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL DE INHABILITACIÓN -->
<div class="modal-overlay" id="delete-modal">
    <div class="modal-container modal-danger-layout">
        <form id="delete-form" method="POST">
            @csrf
            @method('DELETE')
            <div class="modal-header">
                <h3>Inhabilitar Carrera</h3>
            </div>
            <div class="modal-body text-center">
                <div class="danger-icon-wrapper" style="background-color: #fef2f2; color: var(--color-danger-text);">
                    <i class="fa-solid fa-ban"></i>
                </div>
                <p class="delete-warning-text">
                    ¿Está seguro de que desea inhabilitar la carrera <strong id="delete-item-name"></strong>?
                </p>
                <p class="delete-sub-text">Esta acción cambiará el estado de la carrera a inhabilitado, impidiendo que sea utilizada en nuevos procesos institucionales.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
            </div>
        </form>
    </div>
</div>
@endsection

@push('scripts')
    <!-- Script propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/carrera.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Gestión de Denominaciones - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/denominacion.css') }}">
@endpush

@section('content')
<!-- Cabecera de Contenido -->
<header class="main-header">
    <div class="header-welcome">
        <h1>Denominaciones de Actividad</h1>
        <p>Gestión y configuración de los tipos de actividades (clases, talleres, foros, etc.)</p>
    </div>
    <div class="header-date">
        <i class="fa-regular fa-calendar"></i>
        <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
    </div>
</header>

<!-- Contenedor de Notificaciones/Alertas Flotantes -->
<div class="alerts-container">
    @if(session('success'))
        <div class="alert-message success-alert">
            <i class="fa-solid fa-circle-check"></i>
            <span>{{ session('success') }}</span>
        </div>
    @endif

    @if(session('error'))
        <div class="alert-message error-alert">
            <i class="fa-solid fa-circle-xmark"></i>
            <span>{{ session('error') }}</span>
        </div>
    @endif

    @if($errors->any())
        <div class="alert-message error-alert">
            <i class="fa-solid fa-triangle-exclamation"></i>
            <ul style="margin: 0; padding-left: 1.2rem;">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
</div>

<!-- Contenedor Principal de la Tabla -->
<section class="crud-container">

    <!-- Barra Superior de Acciones Filtros -->
    <div class="table-actions-bar">
        <div class="search-wrapper">
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
            <input type="text" placeholder="Buscar denominación..." class="search-input" id="search-denominacion" autocomplete="off">
        </div>

        <div class="filters-wrapper">
            <select class="action-select" id="filter-status">
                <option value="all">Todos los estados</option>
                <option value="active">Habilitados</option>
                <option value="inactive">Inhabilitados</option>
            </select>

            <button class="btn-primary-action" id="btn-open-create">
                <i class="fa-solid fa-plus"></i> Agregar Denominación
            </button>
        </div>
    </div>

    <!-- Tabla de Datos -->
    <div class="table-responsive">
        <table class="crud-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Denominación</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th width="150px">Acción</th>
                </tr>
            </thead>
            <tbody id="table-body">
                @forelse($actividades as $actividad)
                <tr class="data-row" 
                    data-nombre="{{ strtolower($actividad->nombre) }}" 
                    data-descripcion="{{ strtolower($actividad->descripcion ?? '') }}" 
                    data-estado="{{ $actividad->estado ? 'active' : 'inactive' }}">
                    <td><strong>{{ $actividad->id }}</strong></td>
                    <td>
                        <div class="table-primary-text">{{ $actividad->nombre }}</div>
                        <span class="table-secondary-text">Cod: ACT-{{ str_pad($actividad->id, 3, '0', STR_PAD_LEFT) }}</span>
                    </td>
                    <td>{{ $actividad->descripcion ?? 'Sin descripción disponible.' }}</td>
                    <td>
                        @if($actividad->estado)
                            <span class="status-badge status-active">Habilitado</span>
                        @else
                            <span class="status-badge status-inactive">Inhabilitado</span>
                        @endif
                    </td>
                    <td>
                        <div class="action-buttons">
                            <button class="btn-action-edit"
                                onclick="openEditModal('{{ $actividad->id }}', '{{ $actividad->nombre }}', '{{ $actividad->descripcion }}', '{{ $actividad->estado ? 'active' : 'inactive' }}')">
                                <i class="fa-solid fa-pen"></i> Editar
                            </button>
                            <button class="btn-action-delete"
                                onclick="openDeleteModal('{{ $actividad->id }}', '{{ $actividad->nombre }}')">
                                <i class="fa-solid fa-ban"></i>
                            </button>
                        </div>
                    </td>
                </tr>
                @empty
                <tr class="empty-state-row" id="empty-row">
                    <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                        <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                        No hay denominaciones registradas aún.
                    </td>
                </tr>
                @endforelse
                <!-- Fila defensiva para búsquedas vacías en JS -->
                <tr class="empty-state-row" id="no-results-row" style="display: none;">
                    <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                        <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                        No se encontraron resultados que coincidan con la búsqueda.
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    @if(method_exists($actividades, 'hasPages') && $actividades->hasPages())
        <div class="pagination-container" style="margin-top: 15px;">
            {{ $actividades->links() }}
        </div>
    @endif
</section>

<!-- MODAL PARA CREAR -->
<div class="modal-overlay" id="create-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Registrar Denominación</h3>
        </div>
        <!-- CORRECCIÓN: Se actualizó el action con la ruta correcta del grupo 'coordinador.estructura.tipos' -->
        <form id="create-form" action="{{ route('coordinador.estructura.tipos.store') }}" method="POST">
            @csrf
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-tag input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-nombre">Nombre de la Denominación</label>
                        <input type="text" id="create-nombre" name="nombre" placeholder="Ej. Taller" required autocomplete="off">
                    </div>
                </div>

                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="create-descripcion">Descripción</label>
                        <textarea id="create-descripcion" name="descripcion" placeholder="Descripción de la actividad..." rows="3"></textarea>
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-estado">Estado</label>
                        <select id="create-estado" name="estado" required>
                            <option value="active" selected>Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Registrar</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL PARA MODIFICAR -->
<div class="modal-overlay" id="edit-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Modificar Denominación</h3>
        </div>
        <form id="edit-form" method="POST">
            @csrf
            @method('PUT')
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-tag input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-nombre">Nombre de la Denominación</label>
                        <input type="text" id="edit-nombre" name="nombre" required autocomplete="off">
                    </div>
                </div>

                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="edit-descripcion">Descripción</label>
                        <textarea id="edit-descripcion" name="descripcion" rows="3"></textarea>
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-estado">Estado</label>
                        <select id="edit-estado" name="estado" required>
                            <option value="active">Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL DE INHABILITACIÓN -->
<div class="modal-overlay" id="delete-modal">
    <div class="modal-container modal-danger-layout">
        <form id="delete-form" method="POST">
            @csrf
            @method('DELETE')
            <div class="modal-header">
                <h3>Inhabilitar Denominación</h3>
            </div>
            <div class="modal-body text-center">
                <div class="danger-icon-wrapper">
                    <i class="fa-solid fa-circle-exclamation"></i>
                </div>
                <p class="delete-warning-text">
                    ¿Está seguro de que desea inhabilitar la denominación <strong id="delete-item-name"></strong>?
                </p>
                <p class="delete-sub-text">Esta acción cambiará el estado de la denominación a inhabilitada.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
            </div>
        </form>
    </div>
</div>
@endsection

@push('scripts')
<script src="{{ asset('assets/js/estructura/denominacion_de_la_actividad.js') }}"></script>
@endpush
@extends('layouts.app')

@section('title', 'Gestión de Modalidades - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/modalidad.css') }}">
@endpush

@section('content')
<!-- Cabecera de Contenido -->
<header class="main-header">
    <div class="header-welcome">
        <h1>Modalidades de Actividad</h1>
        <p>Gestión y configuración de los entornos de ejecución (Presencial, Virtual, Híbrido, etc.)</p>
    </div>
    <div class="header-date">
        <i class="fa-regular fa-calendar"></i>
        <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
    </div>
</header>

<!-- CONTENEDOR DE ALERTAS -->
<div class="alerts-container">
    @if (session('success'))
        <div class="alert-message success-alert">
            <i class="fa-solid fa-circle-check"></i>
            <span>{{ session('success') }}</span>
        </div>
    @endif

    @if (session('error'))
        <div class="alert-message error-alert">
            <i class="fa-solid fa-circle-xmark"></i>
            <span>{{ session('error') }}</span>
        </div>
    @endif

    @if ($errors->any())
        <div class="alert-message error-alert">
            <i class="fa-solid fa-triangle-exclamation"></i>
            <ul style="margin: 0; padding-left: 1.2rem;">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
</div>

<!-- Contenedor Principal de la Tabla -->
<section class="crud-container">
    
    <!-- Barra Superior de Acciones Filtros -->
    <div class="table-actions-bar">
        <div class="search-wrapper">
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
            <input type="text" placeholder="Buscar modalidad..." class="search-input" id="search-modalidad" autocomplete="off">
        </div>

        <div class="filters-wrapper">
            <select class="action-select" id="filter-status">
                <option value="all">Todos los estados</option>
                <option value="active">Habilitados</option>
                <option value="inactive">Inhabilitados</option>
            </select>

            <button class="btn-primary-action" id="btn-open-create">
                <i class="fa-solid fa-plus"></i> Agregar Modalidad
            </button>
        </div>
    </div>

    <!-- Tabla de Datos -->
    <div class="table-responsive">
        <table class="crud-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Modalidad</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th width="150px">Acción</th>
                </tr>
            </thead>
            <tbody id="table-body">
                @forelse ($modalidades as $modalidad)
                    <tr class="data-row" 
                        data-nombre="{{ strtolower($modalidad->nombre) }}" 
                        data-descripcion="{{ strtolower($modalidad->descripcion ?? '') }}" 
                        data-estado="{{ $modalidad->estado ? 'active' : 'inactive' }}">
                        <td><strong>{{ $modalidad->id }}</strong></td>
                        <td>
                            <div class="table-primary-text">{{ $modalidad->nombre }}</div>
                            <span class="table-secondary-text">Cod: MOD-{{ str_pad($modalidad->id, 3, '0', STR_PAD_LEFT) }}</span>
                        </td>
                        <td>{{ $modalidad->descripcion ?? 'Sin descripción disponible.' }}</td>
                        <td>
                            @if($modalidad->estado)
                                <span class="status-badge status-active">Habilitado</span>
                            @else
                                <span class="status-badge status-inactive">Inhabilitado</span>
                            @endif
                        </td>
                        <td>
                            <div class="action-buttons">
                                <button class="btn-action-edit" 
                                    onclick="openEditModal('{{ $modalidad->id }}', '{{ $modalidad->nombre }}', '{{ $modalidad->descripcion }}', '{{ $modalidad->estado ? 'active' : 'inactive' }}')">
                                    <i class="fa-solid fa-pen"></i> Editar
                                </button>
                                <button class="btn-action-delete" 
                                    onclick="openDeleteModal('{{ $modalidad->id }}', '{{ $modalidad->nombre }}')">
                                    <i class="fa-solid fa-ban"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                @empty
                    <tr class="empty-state-row" id="empty-row">
                        <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No hay modalidades registradas aún.
                        </td>
                    </tr>
                @endforelse
                <!-- Fila defensiva para búsquedas vacías en JS -->
                <tr class="empty-state-row" id="no-results-row" style="display: none;">
                    <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                        <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                        No se encontraron resultados que coincidan con la búsqueda.
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    @if(method_exists($modalidades, 'hasPages') && $modalidades->hasPages())
        <div class="pagination-container" style="margin-top: 15px;">
            {{ $modalidades->links() }}
        </div>
    @endif
</section>

<!-- MODAL PARA CREAR -->
<div class="modal-overlay" id="create-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Registrar Modalidad</h3>
        </div>
        <form id="create-form" action="{{ route('coordinador.estructura.modalidades.store') }}" method="POST">
            @csrf
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-graduation-cap input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-nombre-modalidad">Nombre de la Modalidad</label>
                        <input type="text" id="create-nombre-modalidad" name="nombre" placeholder="Ej. Presencial, Virtual" required autocomplete="off">
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="create-descripcion">Descripción</label>
                        <textarea id="create-descripcion" name="descripcion" placeholder="Describa brevemente el alcance..." rows="3"></textarea>
                    </div>
                </div>
                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-estado">Estado de la Modalidad</label>
                        <select id="create-estado" name="estado" required>
                            <option value="active" selected>Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Registrar</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL PARA MODIFICAR -->
<div class="modal-overlay" id="edit-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Modificar Modalidad</h3>
        </div>
        <form id="edit-form" method="POST">
            @csrf
            @method('PUT')
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-graduation-cap input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-nombre-modalidad">Nombre de la Modalidad</label>
                        <input type="text" id="edit-nombre-modalidad" name="nombre" required autocomplete="off">
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="edit-descripcion">Descripción</label>
                        <textarea id="edit-descripcion" name="descripcion" rows="3"></textarea>
                    </div>
                </div>
                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-estado">Estado de la Modalidad</label>
                        <select id="edit-estado" name="estado" required>
                            <option value="active">Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL DE INHABILITACIÓN -->
<div class="modal-overlay" id="delete-modal">
    <div class="modal-container modal-danger-layout">
        <form id="delete-form" method="POST">
            @csrf
            @method('DELETE')
            <div class="modal-header">
                <h3>Inhabilitar Modalidad</h3>
            </div>
            <div class="modal-body text-center">
                <div class="danger-icon-wrapper">
                    <i class="fa-solid fa-circle-exclamation"></i>
                </div>
                <p class="delete-warning-text">
                    ¿Está seguro de que desea inhabilitar la modalidad <strong id="delete-item-name"></strong>?
                </p>
                <p class="delete-sub-text">Esta acción cambiará el estado de la modalidad a inhabilitada.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
            </div>
        </form>
    </div>
</div>
@endsection

@push('scripts')
    <!-- Script Propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/modalidad.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Planificación Académica')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/coordinador/periodos.css') }}">
@endpush

@section('content')
<!-- Cabecera de Contenido -->
<header class="main-header">
    <div class="header-welcome">
        <h1>Cronograma Académico</h1>
        <p>Gestión operativa y académica del sistema.</p>
    </div>
    <div class="header-date">
        <i class="fa-regular fa-calendar"></i>
        <span>{{ \Carbon\Carbon::now()->translatedFormat('d F, Y') }}</span>
    </div>
</header>

<!-- Contenedor de Notificaciones Flotantes (Toast) -->
<div class="alerts-container">
    @if(session('success'))
        <div class="alert-message success-alert">
            <i class="fa-solid fa-circle-check"></i>
            <span>{{ session('success') }}</span>
        </div>
    @endif

    @if(session('error'))
        <div class="alert-message error-alert">
            <i class="fa-solid fa-circle-xmark"></i>
            <span>{{ session('error') }}</span>
        </div>
    @endif

    @if($errors->any())
        <div class="alert-message error-alert">
            <i class="fa-solid fa-triangle-exclamation"></i>
            <ul style="margin: 0; padding-left: 1.2rem;">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
</div>

<!-- Contenedor Principal de la Tabla -->
<section class="crud-container">

    <!-- Barra Superior de Acciones y Filtros -->
    <div class="table-actions-bar">
        <div class="search-wrapper">
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
            <input type="text" placeholder="Buscar trimestre..." class="search-input" id="search-trimestre" value="{{ request('search') }}" autocomplete="off">
        </div>

        <div class="filters-wrapper">
            <select class="action-select" id="filter-status">
                <option value="all" {{ request('estado') == 'all' ? 'selected' : '' }}>Todos los estados</option>
                <option value="active" {{ request('estado') == 'active' ? 'selected' : '' }}>Habilitados</option>
                <option value="inactive" {{ request('estado') == 'inactive' ? 'selected' : '' }}>Inhabilitados</option>
            </select>

            <button class="btn-primary-action" id="btn-open-create">
                <i class="fa-solid fa-plus"></i> Crear Nuevo Trimestre
            </button>
        </div>
    </div>

    <!-- Tabla de Datos -->
    <div class="table-responsive">
        <table class="crud-table">
            <thead>
                <tr>
                    <th style="padding-left: 2rem;">Identificador del Periodo</th>
                    <th>Fecha de Inicio</th>
                    <th>Fecha de Cierre</th>
                    <th>Estado</th>
                    <th width="180px" style="text-align: center;">Gestión</th>
                </tr>
            </thead>
            <tbody id="table-body">
                <!-- CORRECCIÓN: Se restauró la variable inyectada $periodos para respetar tu ruta activa -->
                @forelse($periodos as $p)
                    @php
                        $hoy = now(); 
                        $inicio = \Carbon\Carbon::parse($p->fecha_inicio); 
                        $fin = \Carbon\Carbon::parse($p->fecha_fin);
                        
                        // Adaptado para usar la propiedad $p->activo de tu base de datos
                        if (!$p->activo) { 
                            $est = "Inhabilitado"; 
                            $claseBadge = "status-inactive"; 
                            $estKey = "inactive";
                        }
                        elseif ($hoy < $inicio) { 
                            $est = "En Espera"; 
                            $claseBadge = "status-waiting"; 
                            $estKey = "waiting";
                        }
                        elseif ($hoy >= $inicio && $hoy <= $fin) { 
                            $est = "Activo"; 
                            $claseBadge = "status-active"; 
                            $estKey = "active";
                        }
                        else { 
                            $est = "Finalizado"; 
                            $claseBadge = "status-finished"; 
                            $estKey = "finished";
                        }
                    @endphp
                    <tr class="data-row" 
                        data-id="{{ $p->id }}" 
                        data-nombre="{{ $p->nombre }}" 
                        data-fecha_inicio="{{ $p->fecha_inicio }}" 
                        data-fecha_fin="{{ $p->fecha_fin }}"
                        data-estado="{{ $p->activo ? 'active' : 'inactive' }}">
                        <td style="padding-left: 2rem;"><strong>{{ $p->nombre }}</strong></td>
                        <td><i class="fa-regular fa-calendar mr-2" style="color: var(--color-text-muted);"></i>{{ $inicio->format('d M, Y') }}</td>
                        <td><i class="fa-regular fa-calendar-check mr-2" style="color: var(--color-text-muted);"></i>{{ $fin->format('d M, Y') }}</td>
                        <td><span class="status-badge {{ $claseBadge }}">{{ $est }}</span></td>
                        <td>
                            <div class="action-buttons" style="justify-content: center;">
                                <button class="btn-action-edit"
                                        data-id="{{ $p->id }}"
                                        data-nombre="{{ $p->nombre }}"
                                        data-fecha_inicio="{{ $p->fecha_inicio }}"
                                        data-fecha_fin="{{ $p->fecha_fin }}"
                                        data-estado="{{ $p->activo ? 'active' : 'inactive' }}">
                                    <i class="fa-solid fa-pen"></i> Editar
                                </button>

                                <!-- Envío de cambio de estado a la ruta existente 'periodos.toggle' -->
                                <form action="{{ route('coordinador.planificacion.periodos.toggle', $p->id) }}" method="POST" style="display: inline;">
                                    @csrf @method('PATCH')
                                    <button type="submit" class="btn-action-delete"
                                            title="{{ $p->activo ? 'Desactivar' : 'Activar' }}"
                                            style="color: {{ $p->activo ? 'var(--color-danger-text)' : 'var(--color-brand-green)' }}; border-color: {{ $p->activo ? 'var(--color-border)' : 'var(--color-border)' }}">
                                        <i class="fa-solid {{ $p->activo ? 'fa-eye-slash' : 'fa-eye' }}"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                @empty
                    <tr class="empty-state-row">
                        <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No existen registros en la planificación.
                        </td>
                    </tr>
                @endforelse
            </tbody>
        </table>
    </div>
    <div class="mt-4">
        <!-- CORRECCIÓN: Se cambió a $periodos para la paginación -->
        {{ $periodos->links() }}
    </div>
</section>

<!-- MODAL PARA CREAR -->
<div class="modal-overlay" id="create-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Registrar Nuevo Periodo</h3>
            <button type="button" class="modal-close" id="btn-close-create-modal" style="background: none; border: none; font-size: 1.6rem; cursor: pointer;">&times;</button>
        </div>
        <form id="create-form" action="{{ route('coordinador.planificacion.periodos.store') }}" method="POST">
            @csrf
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-calendar-days input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-nombre">Nombre del Trimestre</label>
                        <input type="text" name="nombre" id="create-nombre" placeholder="Ej: Trimestre 2024-II" required autocomplete="off">
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-clock input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="create-fecha_inicio">Fecha Apertura</label>
                        <input type="date" name="fecha_inicio" id="create-fecha_inicio" required>
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-clock input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="create-fecha_fin">Fecha Clausura</label>
                        <input type="date" name="fecha_fin" id="create-fecha_fin" required>
                    </div>
                </div>
                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-estado">Estado del Trimestre</label>
                        <select id="create-estado" name="estado" required>
                            <option value="active" selected>Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Registrar</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL PARA MODIFICAR -->
<div class="modal-overlay" id="edit-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Actualizar Configuración de Periodo</h3>
            <button type="button" class="modal-close" id="btn-close-edit-modal" style="background: none; border: none; font-size: 1.6rem; cursor: pointer;">&times;</button>
        </div>
        <form id="edit-form" action="{{ route('coordinador.planificacion.periodos.update', '__ID__') }}" method="POST">
            @csrf
            @method('PUT')
            <input type="hidden" name="id" id="edit-trimestre-id">
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-calendar-days input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-nombre">Nombre del Trimestre</label>
                        <input type="text" name="nombre" id="edit-nombre" required autocomplete="off">
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-clock input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="edit-fecha_inicio">Fecha Apertura</label>
                        <input type="date" name="fecha_inicio" id="edit-fecha_inicio" required>
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-clock input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="edit-fecha_fin">Fecha Clausura</label>
                        <input type="date" name="fecha_fin" id="edit-fecha_fin" required>
                    </div>
                </div>
                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-estado">Estado del Trimestre</label>
                        <select id="edit-estado" name="estado" required>
                            <option value="active">Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>
@endsection

@push('scripts')
    <!-- Script de periodos -->
    <script src="{{ asset('assets/js/coordinador/periodos.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Gestión de ' . $labels['plural'])

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/profesion.css') }}">
@endpush

@section('content')
    <!-- CONTENIDO PRINCIPAL -->
    <!-- Cabecera de Contenido -->
    <header class="main-header">
        <div class="header-welcome">
            <h1>{{ $labels['plural'] }}</h1>
            <p>Gestión y configuración del catálogo de {{ strtolower($labels['plural']) }} para el sistema</p>
        </div>
        <div class="header-date">
            <i class="fa-regular fa-calendar"></i>
            <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
        </div>
    </header>

    <!-- Contenedor de Notificaciones/Alertas Flotantes -->
    <div class="alerts-container">
        @if(session('success'))
            <div class="alert-message success-alert">
                <i class="fa-solid fa-circle-check"></i>
                <span>{{ session('success') }}</span>
            </div>
        @endif

        @if(session('error'))
            <div class="alert-message error-alert">
                <i class="fa-solid fa-circle-xmark"></i>
                <span>{{ session('error') }}</span>
            </div>
        @endif

        @if($errors->any())
            <div class="alert-message error-alert">
                <i class="fa-solid fa-triangle-exclamation"></i>
                <ul style="margin: 0; padding-left: 1.2rem;">
                    @foreach ($errors->all() as $error)
                        <li>{{ $error }}</li>
                    @endforeach
                </ul>
            </div>
        @endif
    </div>

    <!-- Contenedor Principal de la Tabla -->
    <section class="crud-container">

        <!-- Barra Superior de Acciones Filtros -->
        <div class="table-actions-bar">
            <div class="search-wrapper">
                <i class="fa-solid fa-magnifying-glass search-icon"></i>
                <input type="text" placeholder="Buscar {{ strtolower($labels['singular']) }}..." class="search-input" id="search-profesion" autocomplete="off">
            </div>

            <div class="filters-wrapper">
                <select class="action-select" id="filter-status">
                    <option value="all">Todos los estados</option>
                    <option value="active">Habilitados</option>
                    <option value="inactive">Inhabilitados</option>
                </select>

                <button class="btn-primary-action" id="btn-open-create">
                    <i class="fa-solid fa-plus"></i> Agregar {{ $labels['singular'] }}
                </button>
            </div>
        </div>

        <!-- Tabla de Datos -->
        <div class="table-responsive">
            <table class="crud-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>{{ $labels['singular'] }}</th>
                        <th>Descripción</th>
                        <th>Estado</th>
                        <th width="150px">Acción</th>
                    </tr>
                </thead>
                <tbody id="table-body">
                    @forelse($registros as $registro)
                    <tr class="data-row" 
                        data-nombre="{{ strtolower($registro->nombre) }}" 
                        data-descripcion="{{ strtolower($registro->descripcion ?? '') }}" 
                        data-estado="{{ $registro->estado ? 'active' : 'inactive' }}">
                        <td><strong>{{ $registro->id }}</strong></td>
                        <td>
                            <div class="table-primary-text">{{ $registro->nombre }}</div>
                            <span class="table-secondary-text">Cod: PROF-{{ str_pad($registro->id, 3, '0', STR_PAD_LEFT) }}</span>
                        </td>
                        <td>{{ $registro->descripcion ?? 'Sin descripción disponible.' }}</td>
                        <td>
                            @if($registro->estado)
                                <span class="status-badge status-active">Habilitado</span>
                            @else
                                <span class="status-badge status-inactive">Inhabilitado</span>
                            @endif
                        </td>
                        <td>
                            <div class="action-buttons">
                                <button class="btn-action-edit"
                                    onclick="openEditModal('{{ $registro->id }}', '{{ $registro->nombre }}', '{{ $registro->descripcion }}', '{{ $registro->estado ? 'active' : 'inactive' }}')">
                                    <i class="fa-solid fa-pen"></i> Editar
                                </button>
                                <button class="btn-action-delete"
                                    onclick="openDeleteModal('{{ $registro->id }}', '{{ $registro->nombre }}')">
                                    <i class="fa-solid fa-ban"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                    @empty
                    <tr class="empty-state-row" id="empty-row">
                        <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No hay {{ strtolower($labels['plural']) }} registradas aún.
                        </td>
                    </tr>
                    @endforelse
                    <!-- Fila defensiva para búsquedas vacías en JS -->
                    <tr class="empty-state-row" id="no-results-row" style="display: none;">
                        <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No se encontraron resultados que coincidan con la búsqueda.
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

        @if(method_exists($registros, 'hasPages') && $registros->hasPages())
            <div class="pagination-container" style="margin-top: 15px;">
                {{ $registros->links() }}
            </div>
        @endif
    </section>

    <!-- MODAL PARA CREAR -->
    <div class="modal-overlay" id="create-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Registrar {{ $labels['singular'] }}</h3>
            </div>
            <form id="create-form" action="{{ route('coordinador.estructura.store', ['modulo' => $modulo]) }}" method="POST">
                @csrf
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-tag input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-nombre">Nombre de la {{ $labels['singular'] }}</label>
                            <input type="text" id="create-nombre" name="nombre" placeholder="Ej. Nutricionista" required autocomplete="off">
                        </div>
                    </div>

                    <div class="form-group-row align-start">
                        <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                        <div class="input-container">
                            <label for="create-descripcion">Descripción</label>
                            <textarea id="create-descripcion" name="descripcion" placeholder="Descripción..." rows="3"></textarea>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-estado">Estado</label>
                            <select id="create-estado" name="estado" required>
                                <option value="active" selected>Habilitado</option>
                                <option value="inactive">Inhabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Registrar</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL PARA MODIFICAR -->
    <div class="modal-overlay" id="edit-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Modificar {{ $labels['singular'] }}</h3>
            </div>
            <form id="edit-form" method="POST">
                @csrf
                @method('PUT')
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-tag input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-nombre">Nombre de la {{ $labels['singular'] }}</label>
                            <input type="text" id="edit-nombre" name="nombre" required autocomplete="off">
                        </div>
                    </div>

                    <div class="form-group-row align-start">
                        <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                        <div class="input-container">
                            <label for="edit-descripcion">Descripción</label>
                            <textarea id="edit-descripcion" name="descripcion" rows="3"></textarea>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-estado">Estado</label>
                            <select id="edit-estado" name="estado" required>
                                <option value="active">Habilitado</option>
                                <option value="inactive">Inhabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL DE INHABILITACIÓN -->
    <div class="modal-overlay" id="delete-modal">
        <div class="modal-container modal-danger-layout">
            <form id="delete-form" method="POST">
                @csrf
                @method('DELETE')
                <div class="modal-header">
                    <h3>Inhabilitar {{ $labels['singular'] }}</h3>
                </div>
                <div class="modal-body text-center">
                    <div class="danger-icon-wrapper">
                        <i class="fa-solid fa-circle-exclamation"></i>
                    </div>
                    <p class="delete-warning-text">
                        ¿Está seguro de que desea inhabilitar la {{ strtolower($labels['singular']) }} <strong id="delete-item-name"></strong>?
                    </p>
                    <p class="delete-sub-text">Esta acción cambiará el estado de la {{ strtolower($labels['singular']) }} a inhabilitada.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                    <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
                </div>
            </form>
        </div>
    </div>
@endsection

@push('scripts')
    <!-- Script propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/profesion.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Gestión de Salones - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/salon.css') }}">
@endpush

@section('content')
    <!-- CONTENIDO PRINCIPAL -->
    <!-- Cabecera de Contenido -->
    <header class="main-header">
        <div class="header-welcome">
            <h1>Gestión de Salones</h1>
            <p>Configuración y asignación de salones, laboratorios y espacios físicos de la institución</p>
        </div>
        <div class="header-date">
            <i class="fa-regular fa-calendar"></i>
            <span id="current-date-display">{{ date('F, Y') }}</span>
        </div>
    </header>

    <!-- Contenedor Principal de la Tabla -->
    <section class="crud-container">
        
        <!-- Barra Superior de Acciones Filtros (Buscar, Filtrar y Agregar) -->
        <div class="table-actions-bar">
            <div class="search-wrapper">
                <i class="fa-solid fa-magnifying-glass search-icon"></i>
                <input type="text" placeholder="Buscar salón..." class="search-input" id="search-salon">
            </div>

            <div class="filters-wrapper">
                <select class="action-select" id="filter-status">
                    <option value="all">Todos los estados</option>
                    <option value="active">Activos</option>
                    <option value="inactive">Inactivos</option>
                </select>

                <button class="btn-primary-action" id="btn-open-create">
                    <i class="fa-solid fa-plus"></i> Agregar Salón
                </button>
            </div>
        </div>

        <!-- Tabla de Datos -->
        <div class="table-responsive">
            <table class="crud-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Salón / Aula</th>
                        <th>Capacidad</th>
                        <th>Estado</th>
                        <th width="150px">Acción</th>
                    </tr>
                </thead>
                <tbody>
                    @forelse($registros as $espacio)
                    <tr>
                        <td><strong>{{ $espacio->id }}</strong></td>
                        <td>
                            <div class="table-primary-text">{{ $espacio->nombre }}</div>
                            <span class="table-secondary-text">Cod: SAL-0{{ $espacio->id }}</span>
                        </td>
                        <td><strong>{{ $espacio->capacidad }} Estudiantes</strong></td>
                        <td>
                            {{-- Sincronizado con la columna 'estado' (booleana) de la migración --}}
                            @if($espacio->estado)
                                <span class="status-badge status-active">Activo</span>
                            @else
                                <span class="status-badge status-inactive" style="background-color: #f3f4f6; color: #4b5563;">Inactivo</span>
                            @endif
                        </td>
                        <td>
                            <div class="action-buttons">
                                <button class="btn-action-edit" 
                                    onclick="openEditModal('{{ $espacio->id }}', '{{ $espacio->nombre }}', '{{ $espacio->capacidad }}', '{{ $espacio->estado ? 'active' : 'inactive' }}')">
                                        <i class="fa-solid fa-pen"></i> Editar
                                </button>
                                <button class="btn-action-delete" 
                                    onclick="openDeleteModal('{{ $espacio->id }}', '{{ $espacio->nombre }}')">
                                    <i class="fa-solid fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                    @empty
                    <tr>
                        <td colspan="5" style="text-align: center;">No hay salones registrados.</td>
                    </tr>
                    @endforelse
                </tbody>
            </table>
        </div>

        <!-- Paginación inferior -->
        @if(method_exists($registros, 'hasPages') && $registros->hasPages())
            <div class="table-pagination" style="margin-top: 15px;">
                {{ $registros->links() }}
            </div>
        @endif
    </section>

    <!-- MODAL PARA CREAR -->
    <div class="modal-overlay" id="create-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Registrar Salón</h3>
                <button class="modal-close" id="btn-close-create-modal">&times;</button>
            </div>
            <form id="create-form" action="{{ route('coordinador.estructura.store', ['modulo' => $modulo]) }}" method="POST">
                @csrf
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-door-open input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-nombre">Nombre del Salón / Aula</label>
                            <input type="text" id="create-nombre" name="nombre" placeholder="Ej. Laboratorio de Informática" required>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-users input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-capacidad">Capacidad de Estudiantes</label>
                            <input type="number" id="create-capacidad" name="capacidad" min="1" placeholder="Ej. 30" required>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-estado">Estado del Salón</label>
                            <select id="create-estado" name="estado" required>
                                <option value="active" selected>Activo</option>
                                <option value="inactive">Deshabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Registrar</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL PARA MODIFICAR -->
    <div class="modal-overlay" id="edit-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Modificar Salón</h3>
                <button class="modal-close" id="btn-close-edit-modal">&times;</button>
            </div>
            <form id="edit-form" method="POST">
                @csrf
                @method('PUT')
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-door-open input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-nombre">Nombre del Salón / Aula</label>
                            <input type="text" id="edit-nombre" name="nombre" required>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-users input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-capacidad">Capacidad de Estudiantes</label>
                            <input type="number" id="edit-capacidad" name="capacidad" min="1" required>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-estado">Estado del Salón</label>
                            <select id="edit-estado" name="estado" required>
                                <option value="active">Activo</option>
                                <option value="inactive">Deshabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL DE CONFIRMACIÓN DE ELIMINACIÓN -->
    <div class="modal-overlay" id="delete-modal">
        <div class="modal-container modal-danger-layout">
            <form id="delete-form" method="POST">
                @csrf
                @method('DELETE')
                <div class="modal-header">
                    <h3>Inhabilitar Salón</h3>
                    <button type="button" class="modal-close" id="btn-close-delete-modal">&times;</button>
                </div>
                <div class="modal-body text-center">
                    <div class="danger-icon-wrapper">
                        <i class="fa-solid fa-circle-exclamation"></i>
                    </div>
                    <p class="delete-warning-text">
                        ¿Está seguro de que desea inhabilitar el salón <strong id="delete-item-name"></strong>?
                    </p>
                    <p class="delete-sub-text">Esta acción cambiará el estado del salón a deshabilitado.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                    <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
                </div>
            </form>
        </div>
    </div>
@endsection

@push('scripts')
    <!-- Script propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/salon.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Gestión de Tipos de Estudiante - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/tipo_de_estudiante.css') }}">
@endpush

@section('content')
    <!-- CONTENIDO PRINCIPAL -->
    <!-- Cabecera de Contenido -->
    <header class="main-header">
        <div class="header-welcome">
            <h1>Tipos de Estudiante</h1>
            <p>Gestión y configuración de las clasificaciones de estudiantes en el sistema</p>
        </div>
        <div class="header-date">
            <i class="fa-regular fa-calendar"></i>
            <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
        </div>
    </header>

    <!-- Contenedor de Notificaciones/Alertas Flotantes Tipo Toast -->
    <div class="alerts-container">
        @if(session('success'))
            <div class="alert-message success-alert">
                <i class="fa-solid fa-circle-check"></i>
                <span>{{ session('success') }}</span>
            </div>
        @endif

        @if(session('error'))
            <div class="alert-message error-alert">
                <i class="fa-solid fa-circle-xmark"></i>
                <span>{{ session('error') }}</span>
            </div>
        @endif

        @if($errors->any())
            <div class="alert-message error-alert">
                <i class="fa-solid fa-triangle-exclamation"></i>
                <ul style="margin: 0; padding-left: 1.2rem;">
                    @foreach ($errors->all() as $error)
                        <li>{{ $error }}</li>
                    @endforeach
                </ul>
            </div>
        @endif
    </div>

    <!-- Contenedor Principal de la Tabla -->
    <section class="crud-container">
        
        <!-- Barra Superior de Acciones Filtros -->
        <div class="table-actions-bar">
            <div class="search-wrapper">
                <i class="fa-solid fa-magnifying-glass search-icon"></i>
                <input type="text" placeholder="Buscar tipo de estudiante..." class="search-input" id="search-tipo-estudiante" autocomplete="off">
            </div>

            <div class="filters-wrapper">
                <select class="action-select" id="filter-status">
                    <option value="all">Todos los estados</option>
                    <option value="active">Habilitados</option>
                    <option value="inactive">Inhabilitados</option>
                </select>

                <button class="btn-primary-action" id="btn-open-create">
                    <i class="fa-solid fa-plus"></i> Agregar Tipo
                </button>
            </div>
        </div>

        <!-- Tabla de Datos -->
        <div class="table-responsive">
            <table class="crud-table">
                <thead>
                    <tr>
                        <th width="100px">ID</th>
                        <th>Tipo de Estudiante</th>
                        <th>Estado</th>
                        <th width="180px">Acción</th>
                    </tr>
                </thead>
                <tbody id="table-body">
                    @forelse($registros as $tipo)
                        <tr class="data-row" 
                            data-nombre="{{ strtolower($tipo->nombre) }}"
                            data-estado="{{ $tipo->estado ? 'active' : 'inactive' }}">
                            <td><strong>{{ $tipo->id }}</strong></td>
                            <td>
                                <div class="table-primary-text">{{ $tipo->nombre }}</div>
                                <span class="table-secondary-text">Cod: TEST-{{ str_pad($tipo->id, 3, '0', STR_PAD_LEFT) }}</span>
                            </td>
                            <td>
                                @if($tipo->estado)
                                    <span class="status-badge status-active">Habilitado</span>
                                @else
                                    <span class="status-badge status-inactive">Inhabilitado</span>
                                @endif
                            </td>
                            <td>
                                <div class="action-buttons">
                                    <button class="btn-action-edit" onclick="openEditModal('{{ $tipo->id }}', '{{ $tipo->nombre }}', '{{ $tipo->estado ? 'active' : 'inactive' }}')">
                                        <i class="fa-solid fa-pen"></i> Editar
                                    </button>
                                    <button class="btn-action-delete" onclick="openDeleteModal('{{ $tipo->id }}', '{{ $tipo->nombre }}')">
                                        <i class="fa-solid fa-ban"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                    @empty
                        <tr class="empty-state-row" id="empty-row">
                            <td colspan="4" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                                <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                                No se encontraron tipos de estudiante registrados.
                            </td>
                        </tr>
                    @endforelse
                    <!-- Fila defensiva para búsquedas vacías en JS -->
                    <tr class="empty-state-row" id="no-results-row" style="display: none;">
                        <td colspan="4" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No se encontraron resultados que coincidan con la búsqueda.
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

        <!-- Soporte defensivo de paginación unificado -->
        @if(method_exists($registros, 'hasPages') && $registros->hasPages())
            <div class="pagination-container" style="margin-top: 15px;">
                {{ $registros->links() }}
            </div>
        @endif
    </section>

    <!-- MODAL PARA CREAR -->
    <div class="modal-overlay" id="create-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Registrar Tipo de Estudiante</h3>
            </div>
            <form id="create-form" action="{{ route('coordinador.estructura.store', ['modulo' => $modulo]) }}" method="POST">
                @csrf
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-user-graduate input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-nombre">Nombre del Tipo de Estudiante</label>
                            <input type="text" id="create-nombre" name="nombre" placeholder="Ej. Regular" required autocomplete="off">
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-estado">Estado</label>
                            <select id="create-estado" name="estado" required>
                                <option value="active" selected>Habilitado</option>
                                <option value="inactive">Inhabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Registrar</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL PARA MODIFICAR -->
    <div class="modal-overlay" id="edit-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Modificar Tipo de Estudiante</h3>
            </div>
            <form id="edit-form" method="POST">
                @csrf
                @method('PUT')
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-user-graduate input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-nombre">Nombre del Tipo de Estudiante</label>
                            <input type="text" id="edit-nombre" name="nombre" required autocomplete="off">
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-estado">Estado</label>
                            <select id="edit-estado" name="estado" required>
                                <option value="active">Habilitado</option>
                                <option value="inactive">Inhabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL DE INHABILITACIÓN -->
    <div class="modal-overlay" id="delete-modal">
        <div class="modal-container modal-danger-layout">
            <form id="delete-form" method="POST">
                @csrf
                @method('DELETE')
                <div class="modal-header">
                    <h3>Inhabilitar Tipo de Estudiante</h3>
                </div>
                <div class="modal-body text-center">
                    <div class="danger-icon-wrapper">
                        <i class="fa-solid fa-circle-exclamation"></i>
                    </div>
                    <p class="delete-warning-text">
                        ¿Está seguro de que desea inhabilitar el tipo de estudiante <strong id="delete-item-name"></strong>?
                    </p>
                    <p class="delete-sub-text">Esta acción cambiará el estado de este registro a inhabilitado.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                    <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
                </div>
            </form>
        </div>
    </div>
@endsection

@push('scripts')
    <!-- Script propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/tipo_de_estudiante.js') }}"></script>
@endpush
@extends('layouts.app')

@section('title', 'Gestión de Denominaciones - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/denominacion.css') }}">
@endpush

@section('content')
<!-- Cabecera de Contenido -->
<header class="main-header">
    <div class="header-welcome">
        <h1>Denominaciones de Actividad</h1>
        <p>Gestión y configuración de los tipos de actividades (clases, talleres, foros, etc.)</p>
    </div>
    <div class="header-date">
        <i class="fa-regular fa-calendar"></i>
        <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
    </div>
</header>

<!-- Contenedor de Notificaciones/Alertas Flotantes -->
<div class="alerts-container">
    @if(session('success'))
        <div class="alert-message success-alert">
            <i class="fa-solid fa-circle-check"></i>
            <span>{{ session('success') }}</span>
        </div>
    @endif

    @if(session('error'))
        <div class="alert-message error-alert">
            <i class="fa-solid fa-circle-xmark"></i>
            <span>{{ session('error') }}</span>
        </div>
    @endif

    @if($errors->any())
        <div class="alert-message error-alert">
            <i class="fa-solid fa-triangle-exclamation"></i>
            <ul style="margin: 0; padding-left: 1.2rem;">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
</div>

<!-- Contenedor Principal de la Tabla -->
<section class="crud-container">

    <!-- Barra Superior de Acciones Filtros -->
    <div class="table-actions-bar">
        <div class="search-wrapper">
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
            <input type="text" placeholder="Buscar denominación..." class="search-input" id="search-denominacion" autocomplete="off">
        </div>

        <div class="filters-wrapper">
            <select class="action-select" id="filter-status">
                <option value="all">Todos los estados</option>
                <option value="active">Habilitados</option>
                <option value="inactive">Inhabilitados</option>
            </select>

            <button class="btn-primary-action" id="btn-open-create">
                <i class="fa-solid fa-plus"></i> Agregar Denominación
            </button>
        </div>
    </div>

    <!-- Tabla de Datos -->
    <div class="table-responsive">
        <table class="crud-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Denominación</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th width="150px">Acción</th>
                </tr>
            </thead>
            <tbody id="table-body">
                @forelse($actividades as $actividad)
                <tr class="data-row" 
                    data-nombre="{{ strtolower($actividad->nombre) }}" 
                    data-descripcion="{{ strtolower($actividad->descripcion ?? '') }}" 
                    data-estado="{{ $actividad->estado ? 'active' : 'inactive' }}">
                    <td><strong>{{ $actividad->id }}</strong></td>
                    <td>
                        <div class="table-primary-text">{{ $actividad->nombre }}</div>
                        <span class="table-secondary-text">Cod: ACT-{{ str_pad($actividad->id, 3, '0', STR_PAD_LEFT) }}</span>
                    </td>
                    <td>{{ $actividad->descripcion ?? 'Sin descripción disponible.' }}</td>
                    <td>
                        @if($actividad->estado)
                            <span class="status-badge status-active">Habilitado</span>
                        @else
                            <span class="status-badge status-inactive">Inhabilitado</span>
                        @endif
                    </td>
                    <td>
                        <div class="action-buttons">
                            <button class="btn-action-edit"
                                onclick="openEditModal('{{ $actividad->id }}', '{{ $actividad->nombre }}', '{{ $actividad->descripcion }}', '{{ $actividad->estado ? 'active' : 'inactive' }}')">
                                <i class="fa-solid fa-pen"></i> Editar
                            </button>
                            <button class="btn-action-delete"
                                onclick="openDeleteModal('{{ $actividad->id }}', '{{ $actividad->nombre }}')">
                                <i class="fa-solid fa-ban"></i>
                            </button>
                        </div>
                    </td>
                </tr>
                @empty
                <tr class="empty-state-row" id="empty-row">
                    <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                        <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                        No hay denominaciones registradas aún.
                    </td>
                </tr>
                @endforelse
                <!-- Fila defensiva para búsquedas vacías en JS -->
                <tr class="empty-state-row" id="no-results-row" style="display: none;">
                    <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                        <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                        No se encontraron resultados que coincidan con la búsqueda.
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    @if(method_exists($actividades, 'hasPages') && $actividades->hasPages())
        <div class="pagination-container" style="margin-top: 15px;">
            {{ $actividades->links() }}
        </div>
    @endif
</section>

<!-- MODAL PARA CREAR -->
<div class="modal-overlay" id="create-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Registrar Denominación</h3>
        </div>
        <!-- CORRECCIÓN: Se actualizó el action con la ruta correcta del grupo 'coordinador.estructura.tipos' -->
        <form id="create-form" action="{{ route('coordinador.estructura.tipos.store') }}" method="POST">
            @csrf
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-tag input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-nombre">Nombre de la Denominación</label>
                        <input type="text" id="create-nombre" name="nombre" placeholder="Ej. Taller" required autocomplete="off">
                    </div>
                </div>

                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="create-descripcion">Descripción</label>
                        <textarea id="create-descripcion" name="descripcion" placeholder="Descripción de la actividad..." rows="3"></textarea>
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-estado">Estado</label>
                        <select id="create-estado" name="estado" required>
                            <option value="active" selected>Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Registrar</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL PARA MODIFICAR -->
<div class="modal-overlay" id="edit-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Modificar Denominación</h3>
        </div>
        <form id="edit-form" method="POST">
            @csrf
            @method('PUT')
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-tag input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-nombre">Nombre de la Denominación</label>
                        <input type="text" id="edit-nombre" name="nombre" required autocomplete="off">
                    </div>
                </div>

                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="edit-descripcion">Descripción</label>
                        <textarea id="edit-descripcion" name="descripcion" rows="3"></textarea>
                    </div>
                </div>

                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-estado">Estado</label>
                        <select id="edit-estado" name="estado" required>
                            <option value="active">Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL DE INHABILITACIÓN -->
<div class="modal-overlay" id="delete-modal">
    <div class="modal-container modal-danger-layout">
        <form id="delete-form" method="POST">
            @csrf
            @method('DELETE')
            <div class="modal-header">
                <h3>Inhabilitar Denominación</h3>
            </div>
            <div class="modal-body text-center">
                <div class="danger-icon-wrapper">
                    <i class="fa-solid fa-circle-exclamation"></i>
                </div>
                <p class="delete-warning-text">
                    ¿Está seguro de que desea inhabilitar la denominación <strong id="delete-item-name"></strong>?
                </p>
                <p class="delete-sub-text">Esta acción cambiará el estado de la denominación a inhabilitada.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
            </div>
        </form>
    </div>
</div>
@endsection

@push('scripts')
<script src="{{ asset('assets/js/estructura/denominacion_de_la_actividad.js') }}"></script>
@endpush
@extends('layouts.app')

@section('title', 'Gestión de Modalidades - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/modalidad.css') }}">
@endpush

@section('content')
<!-- Cabecera de Contenido -->
<header class="main-header">
    <div class="header-welcome">
        <h1>Modalidades de Actividad</h1>
        <p>Gestión y configuración de los entornos de ejecución (Presencial, Virtual, Híbrido, etc.)</p>
    </div>
    <div class="header-date">
        <i class="fa-regular fa-calendar"></i>
        <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
    </div>
</header>

<!-- CONTENEDOR DE ALERTAS -->
<div class="alerts-container">
    @if (session('success'))
        <div class="alert-message success-alert">
            <i class="fa-solid fa-circle-check"></i>
            <span>{{ session('success') }}</span>
        </div>
    @endif

    @if (session('error'))
        <div class="alert-message error-alert">
            <i class="fa-solid fa-circle-xmark"></i>
            <span>{{ session('error') }}</span>
        </div>
    @endif

    @if ($errors->any())
        <div class="alert-message error-alert">
            <i class="fa-solid fa-triangle-exclamation"></i>
            <ul style="margin: 0; padding-left: 1.2rem;">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
</div>

<!-- Contenedor Principal de la Tabla -->
<section class="crud-container">
    
    <!-- Barra Superior de Acciones Filtros -->
    <div class="table-actions-bar">
        <div class="search-wrapper">
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
            <input type="text" placeholder="Buscar modalidad..." class="search-input" id="search-modalidad" autocomplete="off">
        </div>

        <div class="filters-wrapper">
            <select class="action-select" id="filter-status">
                <option value="all">Todos los estados</option>
                <option value="active">Habilitados</option>
                <option value="inactive">Inhabilitados</option>
            </select>

            <button class="btn-primary-action" id="btn-open-create">
                <i class="fa-solid fa-plus"></i> Agregar Modalidad
            </button>
        </div>
    </div>

    <!-- Tabla de Datos -->
    <div class="table-responsive">
        <table class="crud-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Modalidad</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th width="150px">Acción</th>
                </tr>
            </thead>
            <tbody id="table-body">
                @forelse ($modalidades as $modalidad)
                    <tr class="data-row" 
                        data-nombre="{{ strtolower($modalidad->nombre) }}" 
                        data-descripcion="{{ strtolower($modalidad->descripcion ?? '') }}" 
                        data-estado="{{ $modalidad->estado ? 'active' : 'inactive' }}">
                        <td><strong>{{ $modalidad->id }}</strong></td>
                        <td>
                            <div class="table-primary-text">{{ $modalidad->nombre }}</div>
                            <span class="table-secondary-text">Cod: MOD-{{ str_pad($modalidad->id, 3, '0', STR_PAD_LEFT) }}</span>
                        </td>
                        <td>{{ $modalidad->descripcion ?? 'Sin descripción disponible.' }}</td>
                        <td>
                            @if($modalidad->estado)
                                <span class="status-badge status-active">Habilitado</span>
                            @else
                                <span class="status-badge status-inactive">Inhabilitado</span>
                            @endif
                        </td>
                        <td>
                            <div class="action-buttons">
                                <button class="btn-action-edit" 
                                    onclick="openEditModal('{{ $modalidad->id }}', '{{ $modalidad->nombre }}', '{{ $modalidad->descripcion }}', '{{ $modalidad->estado ? 'active' : 'inactive' }}')">
                                    <i class="fa-solid fa-pen"></i> Editar
                                </button>
                                <button class="btn-action-delete" 
                                    onclick="openDeleteModal('{{ $modalidad->id }}', '{{ $modalidad->nombre }}')">
                                    <i class="fa-solid fa-ban"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                @empty
                    <tr class="empty-state-row" id="empty-row">
                        <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No hay modalidades registradas aún.
                        </td>
                    </tr>
                @endforelse
                <!-- Fila defensiva para búsquedas vacías en JS -->
                <tr class="empty-state-row" id="no-results-row" style="display: none;">
                    <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                        <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                        No se encontraron resultados que coincidan con la búsqueda.
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    @if(method_exists($modalidades, 'hasPages') && $modalidades->hasPages())
        <div class="pagination-container" style="margin-top: 15px;">
            {{ $modalidades->links() }}
        </div>
    @endif
</section>

<!-- MODAL PARA CREAR -->
<div class="modal-overlay" id="create-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Registrar Modalidad</h3>
        </div>
        <form id="create-form" action="{{ route('coordinador.estructura.modalidades.store') }}" method="POST">
            @csrf
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-graduation-cap input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-nombre-modalidad">Nombre de la Modalidad</label>
                        <input type="text" id="create-nombre-modalidad" name="nombre" placeholder="Ej. Presencial, Virtual" required autocomplete="off">
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="create-descripcion">Descripción</label>
                        <textarea id="create-descripcion" name="descripcion" placeholder="Describa brevemente el alcance..." rows="3"></textarea>
                    </div>
                </div>
                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-estado">Estado de la Modalidad</label>
                        <select id="create-estado" name="estado" required>
                            <option value="active" selected>Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Registrar</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL PARA MODIFICAR -->
<div class="modal-overlay" id="edit-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Modificar Modalidad</h3>
        </div>
        <form id="edit-form" method="POST">
            @csrf
            @method('PUT')
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-graduation-cap input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-nombre-modalidad">Nombre de la Modalidad</label>
                        <input type="text" id="edit-nombre-modalidad" name="nombre" required autocomplete="off">
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="edit-descripcion">Descripción</label>
                        <textarea id="edit-descripcion" name="descripcion" rows="3"></textarea>
                    </div>
                </div>
                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-estado">Estado de la Modalidad</label>
                        <select id="edit-estado" name="estado" required>
                            <option value="active">Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL DE INHABILITACIÓN -->
<div class="modal-overlay" id="delete-modal">
    <div class="modal-container modal-danger-layout">
        <form id="delete-form" method="POST">
            @csrf
            @method('DELETE')
            <div class="modal-header">
                <h3>Inhabilitar Modalidad</h3>
            </div>
            <div class="modal-body text-center">
                <div class="danger-icon-wrapper">
                    <i class="fa-solid fa-circle-exclamation"></i>
                </div>
                <p class="delete-warning-text">
                    ¿Está seguro de que desea inhabilitar la modalidad <strong id="delete-item-name"></strong>?
                </p>
                <p class="delete-sub-text">Esta acción cambiará el estado de la modalidad a inhabilitada.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
            </div>
        </form>
    </div>
</div>
@endsection

@push('scripts')
    <!-- Script Propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/modalidad.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Planificación Académica')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/coordinador/periodos.css') }}">
@endpush

@section('content')
<!-- Cabecera de Contenido -->
<header class="main-header">
    <div class="header-welcome">
        <h1>Cronograma Académico</h1>
        <p>Gestión operativa y académica del sistema.</p>
    </div>
    <div class="header-date">
        <i class="fa-regular fa-calendar"></i>
        <span>{{ \Carbon\Carbon::now()->translatedFormat('d F, Y') }}</span>
    </div>
</header>

<!-- Contenedor de Notificaciones Flotantes (Toast) -->
<div class="alerts-container">
    @if(session('success'))
        <div class="alert-message success-alert">
            <i class="fa-solid fa-circle-check"></i>
            <span>{{ session('success') }}</span>
        </div>
    @endif

    @if(session('error'))
        <div class="alert-message error-alert">
            <i class="fa-solid fa-circle-xmark"></i>
            <span>{{ session('error') }}</span>
        </div>
    @endif

    @if($errors->any())
        <div class="alert-message error-alert">
            <i class="fa-solid fa-triangle-exclamation"></i>
            <ul style="margin: 0; padding-left: 1.2rem;">
                @foreach ($errors->all() as $error)
                    <li>{{ $error }}</li>
                @endforeach
            </ul>
        </div>
    @endif
</div>

<!-- Contenedor Principal de la Tabla -->
<section class="crud-container">

    <!-- Barra Superior de Acciones y Filtros -->
    <div class="table-actions-bar">
        <div class="search-wrapper">
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
            <input type="text" placeholder="Buscar trimestre..." class="search-input" id="search-trimestre" value="{{ request('search') }}" autocomplete="off">
        </div>

        <div class="filters-wrapper">
            <select class="action-select" id="filter-status">
                <option value="all" {{ request('estado') == 'all' ? 'selected' : '' }}>Todos los estados</option>
                <option value="active" {{ request('estado') == 'active' ? 'selected' : '' }}>Habilitados</option>
                <option value="inactive" {{ request('estado') == 'inactive' ? 'selected' : '' }}>Inhabilitados</option>
            </select>

            <button class="btn-primary-action" id="btn-open-create">
                <i class="fa-solid fa-plus"></i> Crear Nuevo Trimestre
            </button>
        </div>
    </div>

    <!-- Tabla de Datos -->
    <div class="table-responsive">
        <table class="crud-table">
            <thead>
                <tr>
                    <th style="padding-left: 2rem;">Identificador del Periodo</th>
                    <th>Fecha de Inicio</th>
                    <th>Fecha de Cierre</th>
                    <th>Estado</th>
                    <th width="180px" style="text-align: center;">Gestión</th>
                </tr>
            </thead>
            <tbody id="table-body">
                <!-- CORRECCIÓN: Se restauró la variable inyectada $periodos para respetar tu ruta activa -->
                @forelse($periodos as $p)
                    @php
                        $hoy = now(); 
                        $inicio = \Carbon\Carbon::parse($p->fecha_inicio); 
                        $fin = \Carbon\Carbon::parse($p->fecha_fin);
                        
                        // Adaptado para usar la propiedad $p->activo de tu base de datos
                        if (!$p->activo) { 
                            $est = "Inhabilitado"; 
                            $claseBadge = "status-inactive"; 
                            $estKey = "inactive";
                        }
                        elseif ($hoy < $inicio) { 
                            $est = "En Espera"; 
                            $claseBadge = "status-waiting"; 
                            $estKey = "waiting";
                        }
                        elseif ($hoy >= $inicio && $hoy <= $fin) { 
                            $est = "Activo"; 
                            $claseBadge = "status-active"; 
                            $estKey = "active";
                        }
                        else { 
                            $est = "Finalizado"; 
                            $claseBadge = "status-finished"; 
                            $estKey = "finished";
                        }
                    @endphp
                    <tr class="data-row" 
                        data-id="{{ $p->id }}" 
                        data-nombre="{{ $p->nombre }}" 
                        data-fecha_inicio="{{ $p->fecha_inicio }}" 
                        data-fecha_fin="{{ $p->fecha_fin }}"
                        data-estado="{{ $p->activo ? 'active' : 'inactive' }}">
                        <td style="padding-left: 2rem;"><strong>{{ $p->nombre }}</strong></td>
                        <td><i class="fa-regular fa-calendar mr-2" style="color: var(--color-text-muted);"></i>{{ $inicio->format('d M, Y') }}</td>
                        <td><i class="fa-regular fa-calendar-check mr-2" style="color: var(--color-text-muted);"></i>{{ $fin->format('d M, Y') }}</td>
                        <td><span class="status-badge {{ $claseBadge }}">{{ $est }}</span></td>
                        <td>
                            <div class="action-buttons" style="justify-content: center;">
                                <button class="btn-action-edit"
                                        data-id="{{ $p->id }}"
                                        data-nombre="{{ $p->nombre }}"
                                        data-fecha_inicio="{{ $p->fecha_inicio }}"
                                        data-fecha_fin="{{ $p->fecha_fin }}"
                                        data-estado="{{ $p->activo ? 'active' : 'inactive' }}">
                                    <i class="fa-solid fa-pen"></i> Editar
                                </button>

                                <!-- Envío de cambio de estado a la ruta existente 'periodos.toggle' -->
                                <form action="{{ route('coordinador.planificacion.periodos.toggle', $p->id) }}" method="POST" style="display: inline;">
                                    @csrf @method('PATCH')
                                    <button type="submit" class="btn-action-delete"
                                            title="{{ $p->activo ? 'Desactivar' : 'Activar' }}"
                                            style="color: {{ $p->activo ? 'var(--color-danger-text)' : 'var(--color-brand-green)' }}; border-color: {{ $p->activo ? 'var(--color-border)' : 'var(--color-border)' }}">
                                        <i class="fa-solid {{ $p->activo ? 'fa-eye-slash' : 'fa-eye' }}"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                @empty
                    <tr class="empty-state-row">
                        <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No existen registros en la planificación.
                        </td>
                    </tr>
                @endforelse
            </tbody>
        </table>
    </div>
    <div class="mt-4">
        <!-- CORRECCIÓN: Se cambió a $periodos para la paginación -->
        {{ $periodos->links() }}
    </div>
</section>

<!-- MODAL PARA CREAR -->
<div class="modal-overlay" id="create-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Registrar Nuevo Periodo</h3>
            <button type="button" class="modal-close" id="btn-close-create-modal" style="background: none; border: none; font-size: 1.6rem; cursor: pointer;">&times;</button>
        </div>
        <form id="create-form" action="{{ route('coordinador.planificacion.periodos.store') }}" method="POST">
            @csrf
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-calendar-days input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-nombre">Nombre del Trimestre</label>
                        <input type="text" name="nombre" id="create-nombre" placeholder="Ej: Trimestre 2024-II" required autocomplete="off">
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-clock input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="create-fecha_inicio">Fecha Apertura</label>
                        <input type="date" name="fecha_inicio" id="create-fecha_inicio" required>
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-clock input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="create-fecha_fin">Fecha Clausura</label>
                        <input type="date" name="fecha_fin" id="create-fecha_fin" required>
                    </div>
                </div>
                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="create-estado">Estado del Trimestre</label>
                        <select id="create-estado" name="estado" required>
                            <option value="active" selected>Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Registrar</button>
            </div>
        </form>
    </div>
</div>

<!-- MODAL PARA MODIFICAR -->
<div class="modal-overlay" id="edit-modal">
    <div class="modal-container">
        <div class="modal-header">
            <h3>Actualizar Configuración de Periodo</h3>
            <button type="button" class="modal-close" id="btn-close-edit-modal" style="background: none; border: none; font-size: 1.6rem; cursor: pointer;">&times;</button>
        </div>
        <form id="edit-form" action="{{ route('coordinador.planificacion.periodos.update', '__ID__') }}" method="POST">
            @csrf
            @method('PUT')
            <input type="hidden" name="id" id="edit-trimestre-id">
            <div class="modal-body">
                <div class="form-group-row">
                    <i class="fa-solid fa-calendar-days input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-nombre">Nombre del Trimestre</label>
                        <input type="text" name="nombre" id="edit-nombre" required autocomplete="off">
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-clock input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="edit-fecha_inicio">Fecha Apertura</label>
                        <input type="date" name="fecha_inicio" id="edit-fecha_inicio" required>
                    </div>
                </div>
                <div class="form-group-row align-start">
                    <i class="fa-solid fa-clock input-row-icon mt-xs"></i>
                    <div class="input-container">
                        <label for="edit-fecha_fin">Fecha Clausura</label>
                        <input type="date" name="fecha_fin" id="edit-fecha_fin" required>
                    </div>
                </div>
                <div class="form-group-row">
                    <i class="fa-solid fa-toggle-on input-row-icon"></i>
                    <div class="input-container">
                        <label for="edit-estado">Estado del Trimestre</label>
                        <select id="edit-estado" name="estado" required>
                            <option value="active">Habilitado</option>
                            <option value="inactive">Inhabilitado</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>
@endsection

@push('scripts')
    <!-- Script de periodos -->
    <script src="{{ asset('assets/js/coordinador/periodos.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Gestión de ' . $labels['plural'])

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/profesion.css') }}">
@endpush

@section('content')
    <!-- CONTENIDO PRINCIPAL -->
    <!-- Cabecera de Contenido -->
    <header class="main-header">
        <div class="header-welcome">
            <h1>{{ $labels['plural'] }}</h1>
            <p>Gestión y configuración del catálogo de {{ strtolower($labels['plural']) }} para el sistema</p>
        </div>
        <div class="header-date">
            <i class="fa-regular fa-calendar"></i>
            <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
        </div>
    </header>

    <!-- Contenedor de Notificaciones/Alertas Flotantes -->
    <div class="alerts-container">
        @if(session('success'))
            <div class="alert-message success-alert">
                <i class="fa-solid fa-circle-check"></i>
                <span>{{ session('success') }}</span>
            </div>
        @endif

        @if(session('error'))
            <div class="alert-message error-alert">
                <i class="fa-solid fa-circle-xmark"></i>
                <span>{{ session('error') }}</span>
            </div>
        @endif

        @if($errors->any())
            <div class="alert-message error-alert">
                <i class="fa-solid fa-triangle-exclamation"></i>
                <ul style="margin: 0; padding-left: 1.2rem;">
                    @foreach ($errors->all() as $error)
                        <li>{{ $error }}</li>
                    @endforeach
                </ul>
            </div>
        @endif
    </div>

    <!-- Contenedor Principal de la Tabla -->
    <section class="crud-container">

        <!-- Barra Superior de Acciones Filtros -->
        <div class="table-actions-bar">
            <div class="search-wrapper">
                <i class="fa-solid fa-magnifying-glass search-icon"></i>
                <input type="text" placeholder="Buscar {{ strtolower($labels['singular']) }}..." class="search-input" id="search-profesion" autocomplete="off">
            </div>

            <div class="filters-wrapper">
                <select class="action-select" id="filter-status">
                    <option value="all">Todos los estados</option>
                    <option value="active">Habilitados</option>
                    <option value="inactive">Inhabilitados</option>
                </select>

                <button class="btn-primary-action" id="btn-open-create">
                    <i class="fa-solid fa-plus"></i> Agregar {{ $labels['singular'] }}
                </button>
            </div>
        </div>

        <!-- Tabla de Datos -->
        <div class="table-responsive">
            <table class="crud-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>{{ $labels['singular'] }}</th>
                        <th>Descripción</th>
                        <th>Estado</th>
                        <th width="150px">Acción</th>
                    </tr>
                </thead>
                <tbody id="table-body">
                    @forelse($registros as $registro)
                    <tr class="data-row" 
                        data-nombre="{{ strtolower($registro->nombre) }}" 
                        data-descripcion="{{ strtolower($registro->descripcion ?? '') }}" 
                        data-estado="{{ $registro->estado ? 'active' : 'inactive' }}">
                        <td><strong>{{ $registro->id }}</strong></td>
                        <td>
                            <div class="table-primary-text">{{ $registro->nombre }}</div>
                            <span class="table-secondary-text">Cod: PROF-{{ str_pad($registro->id, 3, '0', STR_PAD_LEFT) }}</span>
                        </td>
                        <td>{{ $registro->descripcion ?? 'Sin descripción disponible.' }}</td>
                        <td>
                            @if($registro->estado)
                                <span class="status-badge status-active">Habilitado</span>
                            @else
                                <span class="status-badge status-inactive">Inhabilitado</span>
                            @endif
                        </td>
                        <td>
                            <div class="action-buttons">
                                <button class="btn-action-edit"
                                    onclick="openEditModal('{{ $registro->id }}', '{{ $registro->nombre }}', '{{ $registro->descripcion }}', '{{ $registro->estado ? 'active' : 'inactive' }}')">
                                    <i class="fa-solid fa-pen"></i> Editar
                                </button>
                                <button class="btn-action-delete"
                                    onclick="openDeleteModal('{{ $registro->id }}', '{{ $registro->nombre }}')">
                                    <i class="fa-solid fa-ban"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                    @empty
                    <tr class="empty-state-row" id="empty-row">
                        <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No hay {{ strtolower($labels['plural']) }} registradas aún.
                        </td>
                    </tr>
                    @endforelse
                    <!-- Fila defensiva para búsquedas vacías en JS -->
                    <tr class="empty-state-row" id="no-results-row" style="display: none;">
                        <td colspan="5" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No se encontraron resultados que coincidan con la búsqueda.
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

        @if(method_exists($registros, 'hasPages') && $registros->hasPages())
            <div class="pagination-container" style="margin-top: 15px;">
                {{ $registros->links() }}
            </div>
        @endif
    </section>

    <!-- MODAL PARA CREAR -->
    <div class="modal-overlay" id="create-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Registrar {{ $labels['singular'] }}</h3>
            </div>
            <form id="create-form" action="{{ route('coordinador.estructura.store', ['modulo' => $modulo]) }}" method="POST">
                @csrf
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-tag input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-nombre">Nombre de la {{ $labels['singular'] }}</label>
                            <input type="text" id="create-nombre" name="nombre" placeholder="Ej. Nutricionista" required autocomplete="off">
                        </div>
                    </div>

                    <div class="form-group-row align-start">
                        <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                        <div class="input-container">
                            <label for="create-descripcion">Descripción</label>
                            <textarea id="create-descripcion" name="descripcion" placeholder="Descripción..." rows="3"></textarea>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-estado">Estado</label>
                            <select id="create-estado" name="estado" required>
                                <option value="active" selected>Habilitado</option>
                                <option value="inactive">Inhabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Registrar</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL PARA MODIFICAR -->
    <div class="modal-overlay" id="edit-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Modificar {{ $labels['singular'] }}</h3>
            </div>
            <form id="edit-form" method="POST">
                @csrf
                @method('PUT')
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-tag input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-nombre">Nombre de la {{ $labels['singular'] }}</label>
                            <input type="text" id="edit-nombre" name="nombre" required autocomplete="off">
                        </div>
                    </div>

                    <div class="form-group-row align-start">
                        <i class="fa-solid fa-align-left input-row-icon mt-xs"></i>
                        <div class="input-container">
                            <label for="edit-descripcion">Descripción</label>
                            <textarea id="edit-descripcion" name="descripcion" rows="3"></textarea>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-estado">Estado</label>
                            <select id="edit-estado" name="estado" required>
                                <option value="active">Habilitado</option>
                                <option value="inactive">Inhabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL DE INHABILITACIÓN -->
    <div class="modal-overlay" id="delete-modal">
        <div class="modal-container modal-danger-layout">
            <form id="delete-form" method="POST">
                @csrf
                @method('DELETE')
                <div class="modal-header">
                    <h3>Inhabilitar {{ $labels['singular'] }}</h3>
                </div>
                <div class="modal-body text-center">
                    <div class="danger-icon-wrapper">
                        <i class="fa-solid fa-circle-exclamation"></i>
                    </div>
                    <p class="delete-warning-text">
                        ¿Está seguro de que desea inhabilitar la {{ strtolower($labels['singular']) }} <strong id="delete-item-name"></strong>?
                    </p>
                    <p class="delete-sub-text">Esta acción cambiará el estado de la {{ strtolower($labels['singular']) }} a inhabilitada.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                    <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
                </div>
            </form>
        </div>
    </div>
@endsection

@push('scripts')
    <!-- Script propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/profesion.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Gestión de Salones - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/salon.css') }}">
@endpush

@section('content')
    <!-- CONTENIDO PRINCIPAL -->
    <!-- Cabecera de Contenido -->
    <header class="main-header">
        <div class="header-welcome">
            <h1>Gestión de Salones</h1>
            <p>Configuración y asignación de salones, laboratorios y espacios físicos de la institución</p>
        </div>
        <div class="header-date">
            <i class="fa-regular fa-calendar"></i>
            <span id="current-date-display">{{ date('F, Y') }}</span>
        </div>
    </header>

    <!-- Contenedor Principal de la Tabla -->
    <section class="crud-container">
        
        <!-- Barra Superior de Acciones Filtros (Buscar, Filtrar y Agregar) -->
        <div class="table-actions-bar">
            <div class="search-wrapper">
                <i class="fa-solid fa-magnifying-glass search-icon"></i>
                <input type="text" placeholder="Buscar salón..." class="search-input" id="search-salon">
            </div>

            <div class="filters-wrapper">
                <select class="action-select" id="filter-status">
                    <option value="all">Todos los estados</option>
                    <option value="active">Activos</option>
                    <option value="inactive">Inactivos</option>
                </select>

                <button class="btn-primary-action" id="btn-open-create">
                    <i class="fa-solid fa-plus"></i> Agregar Salón
                </button>
            </div>
        </div>

        <!-- Tabla de Datos -->
        <div class="table-responsive">
            <table class="crud-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Salón / Aula</th>
                        <th>Capacidad</th>
                        <th>Estado</th>
                        <th width="150px">Acción</th>
                    </tr>
                </thead>
                <tbody>
                    @forelse($registros as $espacio)
                    <tr>
                        <td><strong>{{ $espacio->id }}</strong></td>
                        <td>
                            <div class="table-primary-text">{{ $espacio->nombre }}</div>
                            <span class="table-secondary-text">Cod: SAL-0{{ $espacio->id }}</span>
                        </td>
                        <td><strong>{{ $espacio->capacidad }} Estudiantes</strong></td>
                        <td>
                            {{-- Sincronizado con la columna 'estado' (booleana) de la migración --}}
                            @if($espacio->estado)
                                <span class="status-badge status-active">Activo</span>
                            @else
                                <span class="status-badge status-inactive" style="background-color: #f3f4f6; color: #4b5563;">Inactivo</span>
                            @endif
                        </td>
                        <td>
                            <div class="action-buttons">
                                <button class="btn-action-edit" 
                                    onclick="openEditModal('{{ $espacio->id }}', '{{ $espacio->nombre }}', '{{ $espacio->capacidad }}', '{{ $espacio->estado ? 'active' : 'inactive' }}')">
                                        <i class="fa-solid fa-pen"></i> Editar
                                </button>
                                <button class="btn-action-delete" 
                                    onclick="openDeleteModal('{{ $espacio->id }}', '{{ $espacio->nombre }}')">
                                    <i class="fa-solid fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                    @empty
                    <tr>
                        <td colspan="5" style="text-align: center;">No hay salones registrados.</td>
                    </tr>
                    @endforelse
                </tbody>
            </table>
        </div>

        <!-- Paginación inferior -->
        @if(method_exists($registros, 'hasPages') && $registros->hasPages())
            <div class="table-pagination" style="margin-top: 15px;">
                {{ $registros->links() }}
            </div>
        @endif
    </section>

    <!-- MODAL PARA CREAR -->
    <div class="modal-overlay" id="create-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Registrar Salón</h3>
                <button class="modal-close" id="btn-close-create-modal">&times;</button>
            </div>
            <form id="create-form" action="{{ route('coordinador.estructura.store', ['modulo' => $modulo]) }}" method="POST">
                @csrf
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-door-open input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-nombre">Nombre del Salón / Aula</label>
                            <input type="text" id="create-nombre" name="nombre" placeholder="Ej. Laboratorio de Informática" required>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-users input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-capacidad">Capacidad de Estudiantes</label>
                            <input type="number" id="create-capacidad" name="capacidad" min="1" placeholder="Ej. 30" required>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-estado">Estado del Salón</label>
                            <select id="create-estado" name="estado" required>
                                <option value="active" selected>Activo</option>
                                <option value="inactive">Deshabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Registrar</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL PARA MODIFICAR -->
    <div class="modal-overlay" id="edit-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Modificar Salón</h3>
                <button class="modal-close" id="btn-close-edit-modal">&times;</button>
            </div>
            <form id="edit-form" method="POST">
                @csrf
                @method('PUT')
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-door-open input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-nombre">Nombre del Salón / Aula</label>
                            <input type="text" id="edit-nombre" name="nombre" required>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-users input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-capacidad">Capacidad de Estudiantes</label>
                            <input type="number" id="edit-capacidad" name="capacidad" min="1" required>
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-estado">Estado del Salón</label>
                            <select id="edit-estado" name="estado" required>
                                <option value="active">Activo</option>
                                <option value="inactive">Deshabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL DE CONFIRMACIÓN DE ELIMINACIÓN -->
    <div class="modal-overlay" id="delete-modal">
        <div class="modal-container modal-danger-layout">
            <form id="delete-form" method="POST">
                @csrf
                @method('DELETE')
                <div class="modal-header">
                    <h3>Inhabilitar Salón</h3>
                    <button type="button" class="modal-close" id="btn-close-delete-modal">&times;</button>
                </div>
                <div class="modal-body text-center">
                    <div class="danger-icon-wrapper">
                        <i class="fa-solid fa-circle-exclamation"></i>
                    </div>
                    <p class="delete-warning-text">
                        ¿Está seguro de que desea inhabilitar el salón <strong id="delete-item-name"></strong>?
                    </p>
                    <p class="delete-sub-text">Esta acción cambiará el estado del salón a deshabilitado.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                    <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
                </div>
            </form>
        </div>
    </div>
@endsection

@push('scripts')
    <!-- Script propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/salon.js') }}"></script>
@endpush

@extends('layouts.app')

@section('title', 'Gestión de Tipos de Estudiante - UNEY')

@push('styles')
    <!-- CSS Propio de esta vista -->
    <link rel="stylesheet" href="{{ asset('assets/css/estructura/tipo_de_estudiante.css') }}">
@endpush

@section('content')
    <!-- CONTENIDO PRINCIPAL -->
    <!-- Cabecera de Contenido -->
    <header class="main-header">
        <div class="header-welcome">
            <h1>Tipos de Estudiante</h1>
            <p>Gestión y configuración de las clasificaciones de estudiantes en el sistema</p>
        </div>
        <div class="header-date">
            <i class="fa-regular fa-calendar"></i>
            <span id="current-date-display">{{ now()->translatedFormat('F, Y') }}</span>
        </div>
    </header>

    <!-- Contenedor de Notificaciones/Alertas Flotantes Tipo Toast -->
    <div class="alerts-container">
        @if(session('success'))
            <div class="alert-message success-alert">
                <i class="fa-solid fa-circle-check"></i>
                <span>{{ session('success') }}</span>
            </div>
        @endif

        @if(session('error'))
            <div class="alert-message error-alert">
                <i class="fa-solid fa-circle-xmark"></i>
                <span>{{ session('error') }}</span>
            </div>
        @endif

        @if($errors->any())
            <div class="alert-message error-alert">
                <i class="fa-solid fa-triangle-exclamation"></i>
                <ul style="margin: 0; padding-left: 1.2rem;">
                    @foreach ($errors->all() as $error)
                        <li>{{ $error }}</li>
                    @endforeach
                </ul>
            </div>
        @endif
    </div>

    <!-- Contenedor Principal de la Tabla -->
    <section class="crud-container">
        
        <!-- Barra Superior de Acciones Filtros -->
        <div class="table-actions-bar">
            <div class="search-wrapper">
                <i class="fa-solid fa-magnifying-glass search-icon"></i>
                <input type="text" placeholder="Buscar tipo de estudiante..." class="search-input" id="search-tipo-estudiante" autocomplete="off">
            </div>

            <div class="filters-wrapper">
                <select class="action-select" id="filter-status">
                    <option value="all">Todos los estados</option>
                    <option value="active">Habilitados</option>
                    <option value="inactive">Inhabilitados</option>
                </select>

                <button class="btn-primary-action" id="btn-open-create">
                    <i class="fa-solid fa-plus"></i> Agregar Tipo
                </button>
            </div>
        </div>

        <!-- Tabla de Datos -->
        <div class="table-responsive">
            <table class="crud-table">
                <thead>
                    <tr>
                        <th width="100px">ID</th>
                        <th>Tipo de Estudiante</th>
                        <th>Estado</th>
                        <th width="180px">Acción</th>
                    </tr>
                </thead>
                <tbody id="table-body">
                    @forelse($registros as $tipo)
                        <tr class="data-row" 
                            data-nombre="{{ strtolower($tipo->nombre) }}"
                            data-estado="{{ $tipo->estado ? 'active' : 'inactive' }}">
                            <td><strong>{{ $tipo->id }}</strong></td>
                            <td>
                                <div class="table-primary-text">{{ $tipo->nombre }}</div>
                                <span class="table-secondary-text">Cod: TEST-{{ str_pad($tipo->id, 3, '0', STR_PAD_LEFT) }}</span>
                            </td>
                            <td>
                                @if($tipo->estado)
                                    <span class="status-badge status-active">Habilitado</span>
                                @else
                                    <span class="status-badge status-inactive">Inhabilitado</span>
                                @endif
                            </td>
                            <td>
                                <div class="action-buttons">
                                    <button class="btn-action-edit" onclick="openEditModal('{{ $tipo->id }}', '{{ $tipo->nombre }}', '{{ $tipo->estado ? 'active' : 'inactive' }}')">
                                        <i class="fa-solid fa-pen"></i> Editar
                                    </button>
                                    <button class="btn-action-delete" onclick="openDeleteModal('{{ $tipo->id }}', '{{ $tipo->nombre }}')">
                                        <i class="fa-solid fa-ban"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                    @empty
                        <tr class="empty-state-row" id="empty-row">
                            <td colspan="4" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                                <i class="fa-solid fa-folder-open" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                                No se encontraron tipos de estudiante registrados.
                            </td>
                        </tr>
                    @endforelse
                    <!-- Fila defensiva para búsquedas vacías en JS -->
                    <tr class="empty-state-row" id="no-results-row" style="display: none;">
                        <td colspan="4" style="text-align: center; padding: 3rem; color: var(--color-text-muted);">
                            <i class="fa-solid fa-magnifying-glass" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                            No se encontraron resultados que coincidan con la búsqueda.
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

        <!-- Soporte defensivo de paginación unificado -->
        @if(method_exists($registros, 'hasPages') && $registros->hasPages())
            <div class="pagination-container" style="margin-top: 15px;">
                {{ $registros->links() }}
            </div>
        @endif
    </section>

    <!-- MODAL PARA CREAR -->
    <div class="modal-overlay" id="create-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Registrar Tipo de Estudiante</h3>
            </div>
            <form id="create-form" action="{{ route('coordinador.estructura.store', ['modulo' => $modulo]) }}" method="POST">
                @csrf
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-user-graduate input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-nombre">Nombre del Tipo de Estudiante</label>
                            <input type="text" id="create-nombre" name="nombre" placeholder="Ej. Regular" required autocomplete="off">
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="create-estado">Estado</label>
                            <select id="create-estado" name="estado" required>
                                <option value="active" selected>Habilitado</option>
                                <option value="inactive">Inhabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-create">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Registrar</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL PARA MODIFICAR -->
    <div class="modal-overlay" id="edit-modal">
        <div class="modal-container">
            <div class="modal-header">
                <h3>Modificar Tipo de Estudiante</h3>
            </div>
            <form id="edit-form" method="POST">
                @csrf
                @method('PUT')
                <div class="modal-body">
                    <div class="form-group-row">
                        <i class="fa-solid fa-user-graduate input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-nombre">Nombre del Tipo de Estudiante</label>
                            <input type="text" id="edit-nombre" name="nombre" required autocomplete="off">
                        </div>
                    </div>

                    <div class="form-group-row">
                        <i class="fa-solid fa-toggle-on input-row-icon"></i>
                        <div class="input-container">
                            <label for="edit-estado">Estado</label>
                            <select id="edit-estado" name="estado" required>
                                <option value="active">Habilitado</option>
                                <option value="inactive">Inhabilitado</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-edit">Cancelar</button>
                    <button type="submit" class="modal-btn btn-primary">Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>

    <!-- MODAL DE INHABILITACIÓN -->
    <div class="modal-overlay" id="delete-modal">
        <div class="modal-container modal-danger-layout">
            <form id="delete-form" method="POST">
                @csrf
                @method('DELETE')
                <div class="modal-header">
                    <h3>Inhabilitar Tipo de Estudiante</h3>
                </div>
                <div class="modal-body text-center">
                    <div class="danger-icon-wrapper">
                        <i class="fa-solid fa-circle-exclamation"></i>
                    </div>
                    <p class="delete-warning-text">
                        ¿Está seguro de que desea inhabilitar el tipo de estudiante <strong id="delete-item-name"></strong>?
                    </p>
                    <p class="delete-sub-text">Esta acción cambiará el estado de este registro a inhabilitado.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="modal-btn btn-secondary" id="btn-cancel-delete">Cancelar</button>
                    <button type="submit" class="modal-btn btn-danger">Inhabilitar</button>
                </div>
            </form>
        </div>
    </div>
@endsection

@push('scripts')
    <!-- Script propio de esta vista -->
    <script src="{{ asset('assets/js/estructura/tipo_de_estudiante.js') }}"></script>
@endpush